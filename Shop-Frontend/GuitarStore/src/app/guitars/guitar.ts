import {Category} from '../categories/category';

export class Guitar {
  id: number;
  name: string;
  price: number;
  description: string;
  category: string;
  quantity: number;
  imgUrl: string
}
